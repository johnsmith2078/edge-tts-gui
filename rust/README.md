# Edge TTS GUI Rust

Tauri/Rust rewrite in no-bundle mode.

```powershell
npm install
npm run tauri dev
npm run build:no-bundle
npm run build:compressed
```

The compressed exe is `src-tauri\target\release\edge-tts-gui-rust.exe`.
