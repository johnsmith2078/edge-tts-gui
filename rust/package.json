{
  "name": "edge-tts-gui-rust",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420",
    "build": "vite build",
    "tauri": "tauri",
    "build:no-bundle": "tauri build --no-bundle",
    "build:compressed": "npm run build:no-bundle && powershell -ExecutionPolicy Bypass -File scripts\\compress-exe.ps1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^7.0.0"
  }
}
